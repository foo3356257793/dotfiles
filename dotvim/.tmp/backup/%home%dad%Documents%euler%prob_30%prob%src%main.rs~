fn sum_of_fifth_powers_of_digits(mut a: i64) -> i64
{
    let mut sum = 0;

    while a > 0 {
        sum += (a%10).pow(5);
        a = a / 10;
    }


    return sum;
}

fn prob_30() -> i64 {

    let mut n:u32=4;

    while (n as i64) * 9i64.pow(5) > (10i64.pow(n)-1) {
        n += 1;
    }

    let bd = 10i64.pow(n)-1;

    let mut answer = 0i64;

    for a in 10..bd {
        let sum = sum_of_fifth_powers_of_digits(a);

        if sum == a {
            //println!("{}", a);
            answer += sum;
        }
    }

    return answer;
}

fn main() {
    //println!("Hello, world!");

    let answer = prob_30();

    println!("{}", answer);
}
