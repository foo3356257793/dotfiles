#!/usr/bin/sage

def num_poss(coins, index, target):

    if index == len(coins)-1:

        # can complete
        return 1

    ret = 0

    t = target
    c = coins[index]

    while t >= 0:

        ret += num_poss(coins, index+1, t)
        t -= c

    return ret

if __name__ == "__main__":

    coins = [200,100,50,20,10,5,2,1]

    print(num_poss(coins, 0, 200))
