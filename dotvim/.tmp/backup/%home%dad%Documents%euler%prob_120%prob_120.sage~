#!/usr/bin/sage

if __name__ == "__main__":

    bd = 1000

    S = 0

    for a in range(3,bd+1):

        if a & 1 == 0:

            S += a * 2 * ((a//2) - 1)

        else:

            S += a * (a-1)
