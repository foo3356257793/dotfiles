#include "myflint_d_mat.hpp"

using namespace std;

#include <fmt/core.h>
#include <fmt/ostream.h>

using fmt::print;
using fmt::format;

slong RR_mat::nrows() const { return ((const d_mat_struct*) (*this))->r; }
slong RR_mat::ncols() const { return ((const d_mat_struct*) (*this))->c; }
