#!/usr/bin/sage

if __name__ == "__main__":

    fname = "numbers.txt"

    fp = open(fname, "r")

    numbers = fp.readlines()

    fp.close()

    numbers = [ZZ(num) for num in numbers]

    sum_numbers = sum(numbers)

    ans = f'{sum_numbers}'
    ans = ans[0:10]

    print(sum_numbers)
