pub extern crate num;

fn main() {

    let mut answer = 1i64;

    let UPPER_BOUND = 20;

    for a in 1..(UPPER_BOUND+1) {
        let g = num::integer::gcd(answer, a);
        answer *= a/g;
    }

    println!("{}", answer);
}

