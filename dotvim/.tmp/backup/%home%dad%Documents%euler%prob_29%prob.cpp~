#include <iostream>
using std::cout;
using std::endl;

#include <gmpxx.h>

#include <vector>
#include <algorithm>
using std::vector;
using std::find;

const int A_BOUND = 100;
const int B_BOUND = 100;

void prob_29() {

  vector<mpz_class> distinct_terms = {};

  mpz_class c;
  for(int a=2; a<=A_BOUND; a++)
  {
    for(int b=2; b<=B_BOUND; b++)
    {
      c = a;
      mpz_pow_ui(c.get_mpz_t(), c.get_mpz_t(), b);

      if(find(distinct_terms.begin(), distinct_terms.end(), c) == distinct_terms.end())
      {
        distinct_terms.push_back(c);
      }
    }
  }

  cout << distinct_terms.size() << endl;
}

int main(int argc, char *argv[]) { prob_29(); }
