#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

#include <iostream>

using std::cout;
using std::endl;

#include <gmpxx.h>

mpz_class prob_32()
{
  mpq_class P = 1;

  for(int a=1; a<10; a++)
  {
    for(int b=1; b<10; b++)
    {
      int X = 10*a+b;
      for(int c=1; c<10; c++)
      {
        int Y = 10*c+a;

        if((X!=Y) && (X*c == Y*b))
        {
          if(X < Y)
          {
            P *= X;
            P /= Y;
          }
          else
          {
            P *= Y;
            P /= X;
          }
        }
      }
    }
  }

  return P.get_den();

}

int main(int argc, char *argv[])
{

  mpz_class answer = prob_32();

  gmp_printf("%Zd\n", answer.get_mpz_t());

}

