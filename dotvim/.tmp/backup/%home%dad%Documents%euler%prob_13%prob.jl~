
NUMBER_OF_DIGITS = 10;


lines = readlines("numbers.txt")

data = Vector{BigInt}()

for line in lines
    push!(data, parse(BigInt, line))
end

answer = sum(data)

answer = string(answer)
answer = answer[1:NUMBER_OF_DIGITS]

println(answer)
