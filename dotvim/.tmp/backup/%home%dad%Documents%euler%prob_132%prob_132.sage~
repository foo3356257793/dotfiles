#!/usr/bin/sage

if __name__ == "__main__":

    pr_facs = []

    p = 3
    while len(pr_facs) < 40:

        p = next_prime(p)

        if gcd(p,10) > 1:
            continue

        if power_mod(10,power_mod(10,9,p-1),p) == 1:
            pr_facs.append(p)
            print(p)


