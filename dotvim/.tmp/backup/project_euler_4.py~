def palindrome(num):
    return str(num) == str(num)[::-1]

min = 1
for i in range(999, 100, -1):
  j = i
  prod = i * j
  if prod < min:
    break 
  while prod > min:
    if palindrome(prod):
      min = prod
      break
    else:
      j -= 1
      prod = i * j

print "the maximum is %s" % min
