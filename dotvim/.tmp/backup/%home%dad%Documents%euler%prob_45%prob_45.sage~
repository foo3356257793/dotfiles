#!/usr/bin/sage

def is_hexagonal(a):

    if not is_square(1+8*a):
        return False

    return ZZ(1 + sqrt(1+8*a)) % 4 == 0


if __name__ == "__main__":


    P = lambda x: x*(3*x-1)/2

    k = 166

    p = P(k)

    while not is_hexagonal(p):

        k += 1
        p = P(k)

