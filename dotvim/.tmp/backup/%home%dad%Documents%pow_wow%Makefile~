TEX = xelatex

NAME = pow_wow_20200520

all: $(NAME).html

%.html: %.md
	pandoc -s -c pandoc.css -f markdown -t html -o $@ $+ --metadata

clean:
	rm -rf $(NAME).pdf $(NAME).log $(NAME).aux

.PHONY: clean
