CXX = g++ -std=c++20 -O3
CPPFLAGS =
INCLUDE =
LIBS = -fopenmp -lgmp

PROB = 45

all: prob_$(PROB)

prob_$(PROB): prob.cpp
	$(CXX) $(CPPFLAGS) $(INCLUDE) $+ -o $@ $(LIBS)

cf: prob.cpp
	clang-format -i --style=Google $+

clean:
	rm prob_44

.PHONY: clean

