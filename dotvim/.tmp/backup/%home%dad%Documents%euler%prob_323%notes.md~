Expected number of rounds to hit every bit.

$$
Pr(N <= k)
=
Pr(each fills within k)
=
1 - (Pr(one fills within k))^(1/32)
$$
And
$$
Pr(one fills within k)
=
1 - (1/2)^k
$$
so we have
$$
(1-(1/2)^k)^(1/32)
$$
as the CDF
