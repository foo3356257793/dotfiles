all: lesson.html

%.pdf: %.tex
	$(TEX) $+

lesson.html: lesson.md
	pandoc --standalone -c ../lesson_pandoc.css -f markdown -t html -o $@ $+

clean:
	rm -rf $(NAME).pdf $(NAME).log $(NAME).aux

.PHONY: clean
