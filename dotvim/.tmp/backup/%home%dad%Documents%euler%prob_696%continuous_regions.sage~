#!/usr/bin/sage

def _gen_winning_hands(tile_count, c, remaining):

def gen_winning_hands(c,n):

    tile_count = [1] * c
    remaining = n-c

    if remaining == 0:

        return [tile_count]

    for d0 in range(3):

        if tile_count[0] <= 2:
            # must be triples






if __name__ == "__main__":


