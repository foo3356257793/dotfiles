fn main() {

    const UPPER_BOUND = 20;

    let mut answer = 1i64;

    for a in 1..(UPPER_BOUND+1) {
        let g = num::integer::gcd(answer, a);
        answer *= a/g;
    }

    println!("{}", answer);
}

