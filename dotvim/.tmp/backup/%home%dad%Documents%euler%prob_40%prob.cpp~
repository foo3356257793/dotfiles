#include <algorithm>
#include <iostream>
#include <memory>
#include <numeric>
#include <tuple>
#include <vector>

#include <cmath>

using namespace std;

int64_t prob_40()
{
  auto d = vector<int64_t>();

  int64_t a = 1;

  while(d.size() < 1000000)
  {
    auto a_str = to_string(a);

    for(auto c: a_str)
    {
      d.push_back((int64_t) (c - '0'));
    }

    a++;
  }

  int64_t answer = 1;

  for(int i=0; i<7; i++)
  {
    answer *= d[pow(10,i) - 1];
  }

  return answer;
}

int main() {

  auto answer = prob_40();

  cout << answer << endl;

}

