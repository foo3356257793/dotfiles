#!/usr/bin/sage

if __name__ == "__main__":

    n = 1024
    p = 256

    D = RealDistribution('beta',[p/2,(n-p)/2])

    #variables = var('x')
    f = D.cum_distribution_function

    fun = lambda x : log(x)*(p/2) - log(f(x))

    pl = plot(fun, (x,0,1))
    pl.show()
