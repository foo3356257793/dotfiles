#!/usr/bin/sage

if __name__ == "__main__":

    p = 11

    for i in range(1,p-1):
        line = '%d & ' % i
        for v in range(1,p):
            line += '%d & ' % ((v*i) % p)

        line += '\\\\'
        print line
