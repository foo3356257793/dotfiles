\documentclass[a4paper,10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{amsthm}
\usepackage{amssymb}
\usepackage[T1]{fontenc}
\usepackage{textcomp}
\usepackage{gensymb}
\usepackage{graphicx}

\newtheorem{thm}{Theorem}[section]
\newtheorem{lemma}[thm]{Lemma}
\newtheorem{claim}[thm]{Claim}
\newtheorem{corollary}[thm]{Corollary}
\newtheorem{prop}[thm]{Proposition}
\newtheorem{conj}[thm]{Conjecture}
\theoremstyle{definition}
\newtheorem{remark}[thm]{Remark}
\newtheorem{exa}[thm]{Example}
\newtheorem{defn}[thm]{Definition}

%opening
\title{Matrix Question}
\author{}
\date{}

\begin{document}

\maketitle

\section{}

In the matrix case, there exist $a$ and $b$ analytic such that the
left module generated by $p = 1 + a + b^*$ is not real, but $b \neq
-a$.  Here are $a$ and $b$ which give it:
\[
	a = \begin{pmatrix}
		1&0\\
		0&1 + x_1
	\end{pmatrix}
	\quad \mbox{and} \quad
	b = \begin{pmatrix}
		1 & 0\\
		0 &1 -x_1
	\end{pmatrix}
\]
we see
\[
	(1 + a + b^*) + (1 + a + b^*)^* = \begin{pmatrix}
		6 & 0\\
		0 & 6
	\end{pmatrix}
\]
which is a sum of squares of $(3, 0)$ and $(0, 3)$.  However, $(0,
3)$ is not in the left module generated by $1 + a + b^*$.

\section{}

\subsection{$p$ may have a constant term}

If we allow $p$ to have a constant term, then there exists an
analytic $p$ such that the left module generated by $f = 1 + p + p^*$
is not real:
\[
	p = \begin{pmatrix}
		-1&x\\
		-x&xx
	\end{pmatrix}
\]
gives
\[
	f = \begin{pmatrix}
		-1&x - x^*\\
		-x + x^*&xx + x^*x^* + 1
	\end{pmatrix}
\]
and
\[
	\begin{pmatrix}
		0&0\\
		-x+x^*&1
	\end{pmatrix}f
	=
	\begin{pmatrix}
		0 & 0\\
		0 & 1 + x^*x + xx^*
	\end{pmatrix}
\]
Is is easy for one to show that
\[
	(0, 1) \quad \mbox{and} \quad (0, x) \quad \mbox{and} \quad (0, x^*)
\]
are not in the left module generated by $f$.

\subsection{$p$ may not have a constant term}

\subsubsection{Example}
	If $p$ is homogeneous of degree $1$, then $f = 1 + p + p^*$ is a
	monic linear pencil and hence $I_f$ is real.

\subsubsection{More General}

\[
	\begin{pmatrix}
		b&x-x^*\\
		0&a
	\end{pmatrix}
	\begin{pmatrix}
		1 + xx + x^*x^*&x-x^*\\
		-x + x^*&1
	\end{pmatrix}
	=
	\begin{pmatrix}
		b + bxx + bx^*x^* - xx - x^*x^* + x^*x + xx^*&(b+1)(x-x^*)\\
		(a)(-x+x^*)&a
	\end{pmatrix}
\]
and the symmetrization is
\[
	\begin{pmatrix}
		2b + (2b-2)xx + (2b-2)x^*x^* + 2x^*x + 2xx^*&(b+a+1)(x-x^*)\\
		(b+a+1)(-x+x^*)&2a
	\end{pmatrix}
\]
Lets set this equal to
\[
	\delta_0\begin{pmatrix}
		(\alpha)(-x + x^*) & 1
	\end{pmatrix}^*
	\begin{pmatrix}
		(\alpha)(-x + x^*) & 1
	\end{pmatrix}
	+
	\begin{pmatrix}
		\delta_1 + \delta_2 x^*x + \delta_3 xx^*&0\\
		0&0
	\end{pmatrix}
\]
\[
	=
	\begin{pmatrix}
		\delta_1 + \delta_2 x^*x + \delta_3 xx^* + \delta_0\alpha^2(x^*x
		+ xx^* - xx - x^*x^*)&\delta_0\alpha(x + x^*)\\
		\delta_0\alpha(-x + x^*)& \delta_0
	\end{pmatrix}
\]
So 
\begin{align}
	\delta_0 &= 2a\notag\\
	\delta_0\alpha &= a + b + 1\notag\\
	\delta_0\alpha^2 &= -(2b-2) \notag
\end{align}
Which gives
\[
	\delta_0(\alpha)^2 = 2a\left(\frac{a + b + 1}{2a}\right)^2 = -(2b
	- 2)
\]
\[
	a^2 + 2ab + b^2 + 2a + 2b + 1 = -4ab + 4a
\]
and
\[
	2 = \delta_2 + \delta_0\alpha^2 \quad
	\Rightarrow \quad
	2 \geq -(2b - 2) \quad
	\Rightarrow \quad
	b \geq 0
\]
\end{document}

