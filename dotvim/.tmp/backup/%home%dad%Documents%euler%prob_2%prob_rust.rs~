fn main() {

    const UPPER_BOUND:i64 = 4000000;

    let mut f = 1i64;
    let mut g = 1i64;

    let mut answer = 0i64;

    while g <= UPPER_BOUND {
        if (g%2) == 0 {
            answer += g;
        }

        let h=f+g;
        f=g;
        g=h;
    }

    println!("{}", answer);

}

