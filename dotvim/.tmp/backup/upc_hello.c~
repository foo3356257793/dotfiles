#include <stdio.h>
#include <upc.h>

main()
{
    // one can use THREADS and MYTHREADS like ints
    if( MYTHREAD % 2 ){
        printf("Hello world: I am thread %d and I am even\n", MYTHREAD);
    }
    else{
        printf("Hello world: I am thread %d and I am odd\n", MYTHREAD);
    }
}

