all: test

foo: test.cpp
	g++ -O3 -Wall -shared -std=gnu++20 -fPIC `python3 -m pybind11 --includes` $+ -o $@`python3-config --extension-suffix` -fopenmp

clean:
	rm process_time_log.o

.PHONY: clean

